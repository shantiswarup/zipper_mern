extends layout

block content
  h2 Welcome to #{title}
  h3 Convert your files to ZIP format
  form(action='/upload', method='POST', enctype='multipart/form-data')
    input.input-file(type='file', name='files', multiple='')
    button.input-btn(type='submit') DOWNLOAD ZIP
